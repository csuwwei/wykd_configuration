server:
  port: 9062
  servlet:
    context-path: /wykd-taidigou
spring:
  cloud:
    bus:
      enabled: true
      trace:
        enabled: true
    config:
      label: master
      profile: dev
      uri: http://localhost:8030/wykd-config/
  application:
    name: service-wykdk-taidigou
  rabbitmq:
    host: 120.78.168.84
    port: 5672
    username: guest
    password: guest  
eureka:
#  instance:
#    prefer-ip-address: true
#    instance-id: ${spring.cloud.client.ipAddress}:${server.port}
  client:
    service-url: 
      defaultZone: http://localhost:8010/wykd-eureka/eureka/
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh